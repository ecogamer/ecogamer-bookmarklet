// TODO move this file to /js folder
(function(){

    // Initilisation script should only run once

    // requirejs load all scripts here (or above, outside function, wherever its supposed to go)

    // load css here, any additional items as well (scripts? not sure)

    // Run bookmarklet
    var ecogamerBookmarklet = function() {
        // execute popup
        alert('test');
    };

})();